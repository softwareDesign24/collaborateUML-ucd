name: Generate UML Diagrams from PlantUML

on:
  push:
    branches:
      - main
    paths:
      - '**/*.puml'  # Trigger only if PlantUML files are changed
  pull_request:
    paths:
      - '**/*.puml'

jobs:
  generate-uml:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    
    - name: Set up Docker
      uses: docker/setup-buildx-action@v2
    
    - name: Verify .puml file exists
      run: |
        echo "Checking if the .puml file exists..."
        if [ ! -f use-case.puml ]; then
          echo "Error: use-case.puml not found!"
          exit 1
        fi

    - name: Generate UML diagram using Docker and PlantUML
      run: |
        # Ensure output file name and format are specified
        echo "Generating UML diagram..."
        docker run --rm \
          -v $(pwd):/workspace \
          plantuml/plantuml -tpng /workspace/diagrams/use-case.puml  # Explicitly specify output file format
        
        # List files to check if the diagram is generated
        echo "Listing files after UML generation:"
        ls -l
        
        # Check if the PNG file was generated
        if [ ! -f use-case.png ]; then
          echo "Error: use-case.png not found."
          exit 1
        fi

    - name: Commit generated UML diagram
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        
        # Add the generated PNG file and commit it
        git add use-case.png
        git commit -m "Automated UML diagram generation"
        git push
